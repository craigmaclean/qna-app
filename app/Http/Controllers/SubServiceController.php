<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class SubServiceController extends Controller
{
    // index() — To list all records (GET)
    // create() — To show a form for creating a new record (GET)

    // store() — To save a new record (POST)
    public function store()
    {

    }

    // show() — To display a single record (GET)
    // edit() — To show a form for editing a record (GET)
    // update() — To update an existing record (PUT/PATCH)
    // destroy() — To delete a record (DELETE)
}
